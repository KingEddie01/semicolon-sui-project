import React from 'react';
import WebPage from "@/page-component /webPage/webPage";

const Page = () => {
    return (
        <div className={'flex flex-col min-h-screen'}>
          <WebPage/>
        </div>
    );
};

export default Page;